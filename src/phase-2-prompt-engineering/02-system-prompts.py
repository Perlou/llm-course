"""
系统提示词设计
==============

学习目标：
    1. 理解系统提示词的作用和重要性
    2. 掌握系统提示词的设计原则
    3. 学会构建角色化的系统提示词

核心概念：
    - System Prompt：定义 AI 行为和角色的指令
    - 角色设定：让 AI 扮演特定角色
    - 边界约束：定义 AI 应该和不应该做的事情

前置知识：
    - 01-prompt-anatomy.py

环境要求：
    - pip install openai python-dotenv
"""

import os
from dotenv import load_dotenv
from openai import OpenAI

load_dotenv()


# ==================== 第一部分：系统提示词概述 ====================


def system_prompt_overview():
    """系统提示词概述"""
    print("=" * 60)
    print("第一部分：系统提示词概述")
    print("=" * 60)

    print("""
    什么是系统提示词？
    ─────────────────
    
    系统提示词（System Prompt）是发送给 AI 的特殊指令，
    用于定义 AI 的角色、行为边界和回复风格。
    
    ┌─────────────────────────────────────────────────┐
    │                   消息结构                        │
    ├─────────────────────────────────────────────────┤
    │  System: "你是一个专业的 Python 导师..."          │
    │  ──────────────────────────────────────────     │
    │  User: "如何写一个递归函数？"                     │
    │  ──────────────────────────────────────────     │
    │  Assistant: "递归函数是..."                      │
    └─────────────────────────────────────────────────┘
    
    系统提示词 vs 用户提示词：
    ─────────────────────────
    
    | 特性     | System Prompt    | User Prompt     |
    |---------|------------------|-----------------|
    | 可见性   | 用户通常看不到    | 用户直接输入     |
    | 作用范围 | 整个对话         | 单次请求         |
    | 优先级   | 较高             | 较低            |
    | 用途     | 定义角色和规则    | 具体任务        |
    """)


# ==================== 第二部分：基础系统提示词 ====================


def basic_system_prompt():
    """基础系统提示词演示"""
    print("\n" + "=" * 60)
    print("第二部分：基础系统提示词")
    print("=" * 60)

    client = OpenAI()

    # 无系统提示词
    print("📌 无系统提示词：")
    response1 = client.chat.completions.create(
        model="gpt-3.5-turbo",
        messages=[{"role": "user", "content": "用一句话介绍自己"}],
        max_tokens=100,
    )
    print(f"回复: {response1.choices[0].message.content}")

    # 有系统提示词
    print("\n📌 有系统提示词（设定为海盗角色）：")
    response2 = client.chat.completions.create(
        model="gpt-3.5-turbo",
        messages=[
            {
                "role": "system",
                "content": "你是一个说话像海盗的 AI 助手，总是用海盗的语气回答问题，喜欢说'啊呵'和'宝藏'。",
            },
            {"role": "user", "content": "用一句话介绍自己"},
        ],
        max_tokens=100,
    )
    print(f"回复: {response2.choices[0].message.content}")


# ==================== 第三部分：角色设计模式 ====================


def role_design_patterns():
    """角色设计模式"""
    print("\n" + "=" * 60)
    print("第三部分：角色设计模式")
    print("=" * 60)

    client = OpenAI()

    # 专家角色
    expert_prompt = """你是一位资深的 Python 开发专家，有 15 年编程经验。
你的特点：
- 回答问题时会考虑性能和最佳实践
- 会给出代码示例来说明问题
- 如果问题不清楚会追问
- 回答简洁专业"""

    print("📌 专家角色演示：")
    response = client.chat.completions.create(
        model="gpt-3.5-turbo",
        messages=[
            {"role": "system", "content": expert_prompt},
            {"role": "user", "content": "怎么提高 Python 代码性能？"},
        ],
        max_tokens=300,
    )
    print(f"回复:\n{response.choices[0].message.content}")


# ==================== 第四部分：行为边界设定 ====================


def behavior_boundaries():
    """行为边界设定"""
    print("\n" + "=" * 60)
    print("第四部分：行为边界设定")
    print("=" * 60)

    client = OpenAI()

    boundary_prompt = """你是一个专业的客服助手。

你必须遵守以下规则：
1. 只回答与产品相关的问题
2. 不讨论政治、宗教等敏感话题
3. 如果不知道答案，承认并建议联系人工客服
4. 保持礼貌和专业

你不应该：
- 提供医疗、法律或财务建议
- 透露公司内部信息
- 与用户争论"""

    print("📌 带边界约束的客服：")

    # 正常问题
    response1 = client.chat.completions.create(
        model="gpt-3.5-turbo",
        messages=[
            {"role": "system", "content": boundary_prompt},
            {"role": "user", "content": "你们的退货政策是什么？"},
        ],
        max_tokens=150,
    )
    print(f"正常问题回复: {response1.choices[0].message.content}")

    # 越界问题
    response2 = client.chat.completions.create(
        model="gpt-3.5-turbo",
        messages=[
            {"role": "system", "content": boundary_prompt},
            {"role": "user", "content": "你怎么看美国大选？"},
        ],
        max_tokens=150,
    )
    print(f"\n越界问题回复: {response2.choices[0].message.content}")


# ==================== 第五部分：输出格式控制 ====================


def output_format_control():
    """输出格式控制"""
    print("\n" + "=" * 60)
    print("第五部分：输出格式控制")
    print("=" * 60)

    client = OpenAI()

    format_prompt = """你是一个数据分析助手。

回复格式要求：
1. 使用 Markdown 格式
2. 关键数据用**加粗**
3. 列表项使用项目符号
4. 复杂内容用表格展示
5. 回复控制在 200 字以内"""

    print("📌 格式化输出演示：")
    response = client.chat.completions.create(
        model="gpt-3.5-turbo",
        messages=[
            {"role": "system", "content": format_prompt},
            {"role": "user", "content": "对比一下 Python 和 Java 的特点"},
        ],
        max_tokens=300,
    )
    print(f"回复:\n{response.choices[0].message.content}")


# ==================== 第六部分：练习与思考 ====================


def exercises():
    """练习题"""
    print("\n" + "=" * 60)
    print("练习与思考")
    print("=" * 60)

    print("""
    练习 1：设计专业角色
        为一个"健身教练"设计系统提示词，包含专业知识、沟通风格。

    练习 2：设置安全边界
        为一个"儿童故事助手"设计系统提示词，确保内容适合儿童。

    练习 3：格式控制
        设计一个系统提示词，让 AI 总是用固定格式输出分析报告。

    思考题：
        1. 系统提示词太长会有什么问题？
        2. 如何平衡角色设定和用户自由度？
        3. 系统提示词能被用户覆盖吗？
    """)


# ==================== 主函数 ====================


def main():
    """主函数"""
    print("🚀 系统提示词设计")
    print("=" * 60)
    print("⚠️ 注意：本课程将调用 OpenAI API")
    print("=" * 60)

    api_key = os.getenv("OPENAI_API_KEY")
    if not api_key:
        print("❌ 错误：未设置 OPENAI_API_KEY")
        return

    try:
        system_prompt_overview()
        basic_system_prompt()
        role_design_patterns()
        behavior_boundaries()
        output_format_control()
        exercises()
    except Exception as e:
        print(f"\n❌ 发生错误: {e}")
        return

    print("\n" + "=" * 60)
    print("✅ 课程完成！下一步：03-instruction-tuning.py")
    print("=" * 60)


if __name__ == "__main__":
    main()
