"""
本地 LLM 部署：Ollama 使用
==========================

学习目标：
    1. 了解本地 LLM 的优势和适用场景
    2. 掌握 Ollama 的安装和使用
    3. 学会使用 OpenAI 兼容接口调用本地模型
    4. 了解常用开源模型

核心概念：
    - Ollama：简单易用的本地 LLM 运行工具
    - 开源模型：Llama、Qwen、Mistral 等
    - OpenAI 兼容 API：使用相同代码调用不同模型

前置知识：
    - 完成前面的 API 课程
    - 了解基本的命令行操作

环境要求：
    - 安装 Ollama (https://ollama.ai)
    - pip install openai（用于兼容接口）
"""

import os
import subprocess
from dotenv import load_dotenv

load_dotenv()


# ==================== 第一部分：为什么使用本地 LLM ====================


def why_local_llm():
    """为什么使用本地 LLM"""
    print("=" * 60)
    print("第一部分：为什么使用本地 LLM")
    print("=" * 60)

    print("""
使用本地 LLM 的理由：
┌─────────────────┬──────────────────────────────────────────┐
│ 优势            │ 说明                                     │
├─────────────────┼──────────────────────────────────────────┤
│ 🔒 数据隐私     │ 数据不离开本地，适合敏感信息             │
│ 💰 零 API 成本  │ 只需要硬件成本，无调用费用               │
│ 🌐 离线使用     │ 无需网络连接                             │
│ ⚡ 低延迟       │ 无网络请求延迟                           │
│ 🎛️ 完全控制    │ 可以修改、微调模型                       │
└─────────────────┴──────────────────────────────────────────┘

本地 LLM 的局限：
- 需要较强的 GPU（至少 8GB 显存运行 7B 模型）
- 能力不如最强闭源模型（但差距在缩小）
- 需要自己管理模型文件
    """)


# ==================== 第二部分：Ollama 安装与使用 ====================


def ollama_basics():
    """Ollama 基础使用"""
    print("\n" + "=" * 60)
    print("第二部分：Ollama 安装与使用")
    print("=" * 60)

    print("""
Ollama 安装：

macOS / Linux:
    curl -fsSL https://ollama.ai/install.sh | sh

Windows:
    从 https://ollama.ai 下载安装包

基本命令：
┌────────────────────────────┬────────────────────────────────┐
│ 命令                       │ 说明                           │
├────────────────────────────┼────────────────────────────────┤
│ ollama run llama3.1        │ 下载并运行 Llama 3.1 8B        │
│ ollama run qwen2.5         │ 下载并运行 Qwen 2.5 7B         │
│ ollama list                │ 列出已下载的模型               │
│ ollama pull llama3.1       │ 仅下载模型，不运行             │
│ ollama rm llama3.1         │ 删除模型                       │
│ ollama serve               │ 启动 API 服务                  │
└────────────────────────────┴────────────────────────────────┘
    """)

    # 检查 Ollama 是否安装
    try:
        result = subprocess.run(
            ["ollama", "--version"], capture_output=True, text=True, timeout=5
        )
        if result.returncode == 0:
            print(f"✅ Ollama 已安装: {result.stdout.strip()}")

            # 列出已下载的模型
            result = subprocess.run(
                ["ollama", "list"], capture_output=True, text=True, timeout=5
            )
            if result.stdout.strip():
                print(f"\n📦 已下载的模型:\n{result.stdout}")
            else:
                print("\n📦 尚未下载任何模型")
                print("   运行 'ollama pull llama3.1' 下载模型")
        else:
            print("⚠️ Ollama 未正确安装")
    except FileNotFoundError:
        print("⚠️ Ollama 未安装")
        print("   请访问 https://ollama.ai 下载安装")
    except Exception as e:
        print(f"⚠️ 检查 Ollama 时出错: {e}")


# ==================== 第三部分：常用开源模型 ====================


def popular_models():
    """常用开源模型介绍"""
    print("\n" + "=" * 60)
    print("第三部分：常用开源模型")
    print("=" * 60)

    print("""
推荐模型：
┌──────────────────┬─────────┬────────────┬────────────────────────┐
│ 模型             │ 参数量  │ 显存需求   │ 特点                   │
├──────────────────┼─────────┼────────────┼────────────────────────┤
│ llama3.1:8b      │ 8B      │ ~8GB       │ Meta 官方，综合能力强  │
│ llama3.1:70b     │ 70B     │ ~48GB      │ 接近 GPT-4 水平        │
├──────────────────┼─────────┼────────────┼────────────────────────┤
│ qwen2.5:7b       │ 7B      │ ~8GB       │ 中文能力最强           │
│ qwen2.5:14b      │ 14B     │ ~16GB      │ 中文更强，代码能力好   │
├──────────────────┼─────────┼────────────┼────────────────────────┤
│ mistral          │ 7B      │ ~8GB       │ 速度快，欧洲开发       │
│ mixtral          │ 8x7B    │ ~48GB      │ MoE 架构，效果好       │
├──────────────────┼─────────┼────────────┼────────────────────────┤
│ codellama        │ 7B/34B  │ 8GB/~24GB  │ 代码专用               │
│ deepseek-coder   │ 6.7B    │ ~8GB       │ 代码能力强             │
├──────────────────┼─────────┼────────────┼────────────────────────┤
│ phi3             │ 3.8B    │ ~4GB       │ 微软，小而精           │
│ gemma2           │ 2B/9B   │ ~4GB/~12GB │ Google 开源            │
└──────────────────┴─────────┴────────────┴────────────────────────┘

选择建议：
- 8GB 显存 → llama3.1:8b 或 qwen2.5:7b
- 16GB 显存 → qwen2.5:14b
- 中文为主 → qwen2.5 系列
- 代码为主 → codellama 或 deepseek-coder
- 低配设备 → phi3 或 gemma2:2b
    """)


# ==================== 第四部分：使用 OpenAI 兼容接口 ====================


def openai_compatible_api():
    """使用 OpenAI 兼容接口调用 Ollama"""
    print("\n" + "=" * 60)
    print("第四部分：OpenAI 兼容接口")
    print("=" * 60)

    print("""
💡 Ollama 提供 OpenAI 兼容的 API 接口
   只需修改 base_url，即可复用 OpenAI 的代码！

接口地址：http://localhost:11434/v1
    """)

    # 检查 Ollama 服务是否运行
    try:
        from openai import OpenAI

        client = OpenAI(
            base_url="http://localhost:11434/v1",
            api_key="ollama",  # Ollama 不需要真实的 API key
        )

        print("📤 尝试调用本地模型...")

        # 尝试获取模型列表
        try:
            response = client.chat.completions.create(
                model="qwen2.5:7b",  # 或其他已下载的模型
                messages=[{"role": "user", "content": "你好，请用一句话介绍你自己。"}],
                max_tokens=100,
            )

            print(f"\n📥 收到回复:")
            print(f"   {response.choices[0].message.content}")
            print("\n✅ 本地模型调用成功！")

        except Exception as e:
            if "qwen2.5:7b" in str(e) or "model" in str(e).lower():
                print(f"⚠️ 模型未找到，请先运行: ollama pull qwen2.5:7b")
            else:
                print(f"⚠️ 调用失败: {e}")
                print("   请确保 Ollama 正在运行: ollama serve")

    except Exception as e:
        print(f"⚠️ 无法连接到 Ollama: {e}")
        print("   请确保 Ollama 已安装并运行")

    print("""
代码示例：
─────────────────────────────────────────
from openai import OpenAI

# 只需修改 base_url 即可使用本地模型
client = OpenAI(
    base_url="http://localhost:11434/v1",
    api_key="ollama"
)

response = client.chat.completions.create(
    model="llama3.1",  # 使用 Ollama 中的模型名
    messages=[
        {"role": "user", "content": "你好"}
    ]
)
print(response.choices[0].message.content)
─────────────────────────────────────────
    """)


# ==================== 第五部分：模型切换封装 ====================


def model_switching():
    """封装模型切换逻辑"""
    print("\n" + "=" * 60)
    print("第五部分：模型切换封装")
    print("=" * 60)

    print("""
💡 实用技巧：封装一个支持多模型的客户端

─────────────────────────────────────────────────────────────
from openai import OpenAI
import os

def get_llm_client(provider="openai"):
    '''获取 LLM 客户端'''
    
    configs = {
        "openai": {
            "base_url": "https://api.openai.com/v1",
            "api_key": os.getenv("OPENAI_API_KEY"),
            "model": "gpt-3.5-turbo"
        },
        "ollama": {
            "base_url": "http://localhost:11434/v1",
            "api_key": "ollama",
            "model": "llama3.1"
        },
        "deepseek": {
            "base_url": "https://api.deepseek.com/v1",
            "api_key": os.getenv("DEEPSEEK_API_KEY"),
            "model": "deepseek-chat"
        }
    }
    
    config = configs.get(provider, configs["openai"])
    client = OpenAI(
        base_url=config["base_url"],
        api_key=config["api_key"]
    )
    return client, config["model"]

# 使用示例
client, model = get_llm_client("ollama")
response = client.chat.completions.create(
    model=model,
    messages=[{"role": "user", "content": "你好"}]
)
─────────────────────────────────────────────────────────────

这样只需修改 provider 参数，就能切换不同的模型！
    """)


# ==================== 第六部分：练习与思考 ====================


def exercises():
    """练习题"""
    print("\n" + "=" * 60)
    print("练习与思考")
    print("=" * 60)

    exercises_text = """
练习 1：模型对比
    下载 llama3.1 和 qwen2.5，对同一问题进行测试，
    对比中英文回答质量。

练习 2：性能测试
    测量本地模型的：
    - 首字延迟 (TTFT)
    - 生成速度 (tokens/s)
    并与 OpenAI API 对比。

练习 3：封装 LLM 服务
    基于第五部分的代码，创建一个完整的 LLMService 类，
    支持 OpenAI、Claude、Gemini 和 Ollama。

练习 4：量化模型
    尝试下载量化版本的模型（如 llama3.1:8b-q4_0），
    对比与原版的差异。

思考题：
    1. 为什么 Ollama 可以提供 OpenAI 兼容接口？
    2. 本地模型和云端 API 的延迟差异主要来自哪里？
    3. 什么情况下应该选择本地部署而不是 API 调用？
    """
    print(exercises_text)


# ==================== 主函数 ====================


def main():
    """主函数"""
    print("🚀 本地 LLM 部署：Ollama 使用")
    print("=" * 60)
    print("💡 本课程介绍如何使用 Ollama 运行本地大模型")
    print("=" * 60)

    try:
        why_local_llm()
        ollama_basics()
        popular_models()
        openai_compatible_api()
        model_switching()
        exercises()
    except Exception as e:
        print(f"\n❌ 发生错误: {e}")
        return

    print("\n" + "=" * 60)
    print("✅ 课程完成！")
    print("下一步：07-error-handling.py（错误处理与重试）")
    print("=" * 60)


if __name__ == "__main__":
    main()
