"""
OpenAI API 参数详解
==================

学习目标：
    1. 理解 temperature 参数对输出的影响
    2. 掌握 top_p 核采样的原理
    3. 学会使用 max_tokens 控制输出长度
    4. 了解 frequency_penalty 和 presence_penalty

核心概念：
    - Temperature：控制输出的随机性/创造性
    - Top_p：核采样，另一种控制随机性的方式
    - Max_tokens：限制输出的最大 token 数
    - Penalty 参数：控制词汇重复

前置知识：
    - 完成 01-openai-api-basics.py

环境要求：
    - pip install openai python-dotenv
"""

import os
from dotenv import load_dotenv
from openai import OpenAI

load_dotenv()


# ==================== 第一部分：Temperature 参数 ====================


def explore_temperature():
    """探索 temperature 参数的影响"""
    print("=" * 60)
    print("第一部分：Temperature 参数")
    print("=" * 60)

    client = OpenAI()

    print("""
Temperature 参数说明：
┌────────────────┬─────────────────────────────────────┐
│ 值             │ 效果                                 │
├────────────────┼─────────────────────────────────────┤
│ 0.0            │ 确定性输出，每次结果相同             │
│ 0.5 - 0.7      │ 平衡创造性和一致性（推荐默认值）     │
│ 1.0            │ 更具创造性，输出多样                 │
│ 1.5 - 2.0      │ 高度随机，可能不连贯                 │
└────────────────┴─────────────────────────────────────┘
    """)

    prompt = "用一个词描述太阳"
    temperatures = [0.0, 0.5, 1.0, 1.5]

    print(f"📝 测试提示词: '{prompt}'")
    print("-" * 40)

    for temp in temperatures:
        print(f"\n🌡️ Temperature = {temp}")

        # 同一个 temperature 调用 3 次，观察一致性
        results = []
        for i in range(3):
            response = client.chat.completions.create(
                model="gpt-3.5-turbo",
                messages=[{"role": "user", "content": prompt}],
                temperature=temp,
                max_tokens=20,
            )
            results.append(response.choices[0].message.content.strip())

        for i, result in enumerate(results, 1):
            print(f"   第{i}次: {result}")

    print("\n💡 观察：temperature=0 时，输出完全一致；值越高，变化越大")


# ==================== 第二部分：Top_p 核采样 ====================


def explore_top_p():
    """探索 top_p 参数"""
    print("\n" + "=" * 60)
    print("第二部分：Top_p 核采样")
    print("=" * 60)

    client = OpenAI()

    print("""
Top_p 参数说明：
- 也称为"核采样"(nucleus sampling)
- 控制从概率最高的 tokens 中采样
- top_p=0.1: 只从累计概率前 10% 的 tokens 采样
- top_p=1.0: 考虑所有 tokens

⚠️ 注意：一般只调整 temperature 或 top_p 其中之一
    """)

    prompt = "写一个关于月亮的短句"
    top_p_values = [0.1, 0.5, 0.9]

    print(f"📝 测试提示词: '{prompt}'")
    print("-" * 40)

    for top_p in top_p_values:
        print(f"\n🎯 Top_p = {top_p}")

        for i in range(2):
            response = client.chat.completions.create(
                model="gpt-3.5-turbo",
                messages=[{"role": "user", "content": prompt}],
                temperature=1.0,  # 固定 temperature
                top_p=top_p,
                max_tokens=30,
            )
            print(f"   第{i + 1}次: {response.choices[0].message.content.strip()}")

    print("\n💡 top_p 越小，输出越保守；越大，越多样")


# ==================== 第三部分：Max_tokens 参数 ====================


def explore_max_tokens():
    """探索 max_tokens 参数"""
    print("\n" + "=" * 60)
    print("第三部分：Max_tokens 参数")
    print("=" * 60)

    client = OpenAI()

    print("""
Max_tokens 参数说明：
- 限制 AI 回复的最大 token 数
- 如果回复被截断，finish_reason 会是 'length'
- 1 个中文字符约 1-2 tokens
- 1 个英文单词约 1-2 tokens
    """)

    prompt = "请详细解释什么是人工智能，包括其历史、应用和未来发展。"
    max_tokens_values = [20, 50, 200]

    print(f"📝 测试提示词: '{prompt}'")
    print("-" * 40)

    for max_tokens in max_tokens_values:
        response = client.chat.completions.create(
            model="gpt-3.5-turbo",
            messages=[{"role": "user", "content": prompt}],
            max_tokens=max_tokens,
        )

        content = response.choices[0].message.content
        finish_reason = response.choices[0].finish_reason

        print(f"\n📏 Max_tokens = {max_tokens}")
        print(f"   回复: {content[:100]}{'...' if len(content) > 100 else ''}")
        print(f"   实际 tokens: {response.usage.completion_tokens}")
        print(f"   结束原因: {finish_reason}")

        if finish_reason == "length":
            print("   ⚠️ 回复被截断!")


# ==================== 第四部分：Penalty 参数 ====================


def explore_penalty_params():
    """探索 frequency_penalty 和 presence_penalty"""
    print("\n" + "=" * 60)
    print("第四部分：Penalty 参数")
    print("=" * 60)

    client = OpenAI()

    print("""
Penalty 参数说明：
┌────────────────────┬──────────────────────────────────────┐
│ 参数               │ 作用                                 │
├────────────────────┼──────────────────────────────────────┤
│ frequency_penalty  │ 降低重复出现的词的概率               │
│                    │ 值越高，越不容易重复相同的词         │
├────────────────────┼──────────────────────────────────────┤
│ presence_penalty   │ 鼓励模型谈论新话题                   │
│                    │ 值越高，越倾向使用新词汇             │
└────────────────────┴──────────────────────────────────────┘

取值范围：-2.0 到 2.0
    """)

    prompt = "列出 10 种你喜欢的颜色"

    configs = [
        {"frequency_penalty": 0.0, "presence_penalty": 0.0, "desc": "默认 (无惩罚)"},
        {"frequency_penalty": 2.0, "presence_penalty": 0.0, "desc": "高频率惩罚"},
        {"frequency_penalty": 0.0, "presence_penalty": 2.0, "desc": "高存在惩罚"},
    ]

    print(f"📝 测试提示词: '{prompt}'")
    print("-" * 40)

    for config in configs:
        response = client.chat.completions.create(
            model="gpt-3.5-turbo",
            messages=[{"role": "user", "content": prompt}],
            frequency_penalty=config["frequency_penalty"],
            presence_penalty=config["presence_penalty"],
        )

        print(f"\n🔧 {config['desc']}")
        print(
            f"   frequency_penalty={config['frequency_penalty']}, presence_penalty={config['presence_penalty']}"
        )
        print(f"   回复: {response.choices[0].message.content}")


# ==================== 第五部分：参数组合建议 ====================


def parameter_recommendations():
    """不同场景下的参数组合建议"""
    print("\n" + "=" * 60)
    print("第五部分：参数组合建议")
    print("=" * 60)

    print("""
📋 不同场景推荐参数：

┌──────────────────┬─────────────┬────────┬─────────────────────┐
│ 场景             │ temperature │ top_p  │ 说明                │
├──────────────────┼─────────────┼────────┼─────────────────────┤
│ 代码生成         │ 0.0 - 0.2   │ 1.0    │ 需要精确性          │
│ 数据提取/解析    │ 0.0         │ 1.0    │ 需要确定性结果      │
│ 翻译             │ 0.2 - 0.4   │ 1.0    │ 保持准确但有灵活    │
│ 客服对话         │ 0.5 - 0.7   │ 1.0    │ 自然但可控          │
│ 创意写作         │ 0.8 - 1.2   │ 0.9    │ 需要多样性          │
│ 头脑风暴         │ 1.0 - 1.5   │ 0.8    │ 最大创造性          │
└──────────────────┴─────────────┴────────┴─────────────────────┘

💡 小贴士：
1. 先从默认值开始（temperature=1.0, top_p=1.0）
2. 根据需要调整一个参数，不要同时调整多个
3. 代码和数据任务用低 temperature
4. 创意任务可以提高 temperature
5. 使用 max_tokens 防止输出过长
    """)


# ==================== 第六部分：练习与思考 ====================


def exercises():
    """练习题"""
    print("\n" + "=" * 60)
    print("练习与思考")
    print("=" * 60)

    exercises_text = """
练习 1：参数对比实验
    选择一个任务（如写诗、写代码、回答问题），
    分别使用不同的 temperature 值（0, 0.5, 1.0, 1.5），
    记录输出质量和一致性的变化。

练习 2：找到最佳参数
    假设你要构建一个代码生成助手，
    实验找出最适合生成 Python 代码的参数组合。

练习 3：避免重复
    让 AI 写一篇 200 字的故事，
    使用 frequency_penalty 减少词汇重复。

思考题：
    1. 为什么 temperature=0 时仍然叫"采样"？它和 argmax 有什么区别？
    2. 在什么情况下，你会选择调整 top_p 而不是 temperature？
    """
    print(exercises_text)


# ==================== 主函数 ====================


def main():
    """主函数"""
    print("🚀 OpenAI API 参数详解")
    print("=" * 60)
    print("⚠️ 注意：本课程将多次调用 API，预估消耗约 2000-3000 tokens")
    print("=" * 60)

    api_key = os.getenv("OPENAI_API_KEY")
    if not api_key:
        print("❌ 请先配置 OPENAI_API_KEY 环境变量")
        return

    try:
        explore_temperature()
        explore_top_p()
        explore_max_tokens()
        explore_penalty_params()
        parameter_recommendations()
        exercises()
    except Exception as e:
        print(f"\n❌ 发生错误: {e}")
        return

    print("\n" + "=" * 60)
    print("✅ 课程完成！")
    print("下一步：03-streaming-responses.py（流式响应）")
    print("=" * 60)


if __name__ == "__main__":
    main()
